language: bash
dist: disco

script:
  - support/automate.sh

deploy:
  provider: releases
  skip_cleanup: true
  file_glob: true
  file:
    - "dist/*.*"

branches:
  except:
    - # Do not build tags that we create when we upload to GitHub Releases
    - /^(?i:continuous.*)$/